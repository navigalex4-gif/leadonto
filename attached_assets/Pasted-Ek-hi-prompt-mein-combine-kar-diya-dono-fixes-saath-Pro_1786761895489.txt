Ek hi prompt mein combine kar diya — dono fixes saath:
Prompt for Replit:
"Two fixes needed on the live site:
1. Fix Google Sign-In failing inside embedded WebViews (ad in-app browsers)
Most real traffic is opening the site inside an embedded WebView (Facebook/Instagram in-app browser, or similar) — detectable via ; wv) in the user agent, or FBAN/FBAV/Instagram markers. Google blocks 'Sign in with Google' inside WebViews as a security policy, so these users hit a dead end silently.
Detect WebView context (user agent check for ; wv) or FBAN/FBAV/Instagram).
When detected, hide/de-prioritize the 'Continue with Google' button and make Email OTP the primary signup method instead.
Show a small banner when in WebView: 'For the best experience, open this in your browser' with an 'Open in Chrome' / copy-link option.
Add analytics event oauth_blocked_webview when this is detected, so we can measure how much traffic this affects.
Also add error handling + analytics events through the full signup flow: signup_form_viewed, otp_requested, otp_verify_attempted, otp_verify_success / otp_verify_failed (with error reason as a property), account_created — right now failures happen with zero visibility.
2. Add above-the-fold CTA to inner landing pages (currently only the homepage has this)
/interview-ace, /resume-intelligence, /english-guru, /communication-check, /rozgar-samachar, and /tools-pro all receive direct paid ad traffic (users land here first, not on the homepage), but none of them have a primary CTA button visible without scrolling on mobile (≤768px width) — same problem the homepage had before its fix.
On each of these pages, add a single clear primary CTA button visible in the first viewport on mobile, matching that page's core action:
/interview-ace → 'Start Free Mock Interview'
/resume-intelligence → 'Scan My Resume Free'
/english-guru → 'Start Speaking Practice'
/communication-check → 'Try My Free 90-Second Check' (should already exist — confirm it's above the fold here too, not just linked from homepage)
/rozgar-samachar → 'Browse Jobs Now'
/tools-pro → 'Explore Tools Free'
Reorder each page's mobile layout if needed so the CTA sits right after the headline/subheadline, before any long-form content, quotes, or feature lists.
Test each page on a 375px width viewport to confirm the CTA is visible without scrolling.
Don't change backend logic beyond the analytics events and WebView detection listed above."
Deploy karne ke baad 24 ghante traffic chalne dena, fir fresh CSV — teeno cheezein check karenge: WebView OTP conversions, inner-page bounce, aur signup completion rate.